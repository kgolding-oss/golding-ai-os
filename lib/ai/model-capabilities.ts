import type { ModelDefinition } from "./model-types"; export function modelSupports(model:ModelDefinition, capability:keyof ModelDefinition["capabilities"]){return Boolean(model.capabilities[capability]);} export function summarizeModelCapabilities(model:ModelDefinition){return [model.capabilities.reasoning&&"reasoning",model.capabilities.structuredOutput&&"structured output",model.capabilities.streaming&&"streaming",model.capabilities.tools&&"tools",model.capabilities.vision&&"vision",model.capabilities.embedding&&"embeddings"].filter(Boolean) as string[];}
