import type { AiExecutionRequest, ModelDefinition } from "./model-types"; import { modelRegistry } from "./model-registry"; export function routeModel(req:AiExecutionRequest){if(req.modelId){const m=modelRegistry.get(req.modelId); if(m)return {model:m,reason:"requested model"};} const candidates=modelRegistry.active().filter(m=>!req.tools?.length||m.capabilities.tools); const model=candidates.sort((a,b)=>a.cost.inputTokenUsd-b.cost.inputTokenUsd||b.contextWindow-a.contextWindow)[0]; if(!model) throw new Error("No registered model can satisfy the request."); return {model,reason:"lowest cost capable active model"};}
