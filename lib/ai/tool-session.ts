export type ToolSession={id:string;toolId:string;status:"pending"|"completed"|"failed";createdAt:string;latencyMs?:number}; const sessions:ToolSession[]=[]; export function recordToolSession(toolId:string,status:ToolSession["status"],latencyMs=0){const s={id:`tool_sess_${sessions.length+1}`,toolId,status,createdAt:new Date().toISOString(),latencyMs}; sessions.unshift(s); return s;} export function listToolSessions(){return sessions;}
