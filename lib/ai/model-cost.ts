import type { ModelDefinition } from "./model-types"; export function estimateTokens(text:string){return Math.max(1,Math.ceil(text.length/4));} export function estimateModelCost(model:ModelDefinition,inputTokens:number,outputTokens=0){const divisor=model.cost.unit==="1M_TOKENS"?1_000_000:1_000; return Number(((inputTokens/divisor)*model.cost.inputTokenUsd+(outputTokens/divisor)*model.cost.outputTokenUsd).toFixed(6));}
