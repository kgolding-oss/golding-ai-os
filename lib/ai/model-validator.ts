import type { ModelDefinition } from "./model-types";
export function validateModel(model:ModelDefinition){const errors:string[]=[]; if(!model.id)errors.push("Model id is required."); if(!model.provider)errors.push("Model provider is required."); if(!model.family)errors.push("Model family is required."); if(model.contextWindow<=0)errors.push("Context window must be positive."); if(!model.capabilities?.modalities?.length)errors.push("At least one modality is required."); if(model.cost.inputTokenUsd<0||model.cost.outputTokenUsd<0)errors.push("Model cost cannot be negative."); return {valid:errors.length===0,errors};}
