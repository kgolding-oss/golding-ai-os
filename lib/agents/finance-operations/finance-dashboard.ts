import type { FinanceSnapshot } from "./finance-types"; export function buildFinanceDashboard(s:FinanceSnapshot){return {budgetSummary:s.budgets,cashPosition:s.forecasts[0]?.closingCash??0,cashForecast:s.forecasts,donationActivity:s.donations,grantRevenue:s.grants,sponsorRevenue:s.sponsors,outstandingInvoices:s.invoices.filter(i=>i.status!=="paid"),upcomingCompliance:s.compliance,financialRisks:s.recommendations};}
