import type { FinanceTelemetryEvent } from "./finance-types"; class FinanceTelemetry{private events:FinanceTelemetryEvent[]=[];record(kind:string,message:string,metadata?:Record<string,unknown>){this.events.push({id:`fin-${this.events.length+1}`,kind,at:new Date().toISOString(),message,metadata});}summary(){return {events:this.events.length,recent:this.events.slice(-10).reverse()};}} export const financeTelemetry=new FinanceTelemetry();
