import type { GrantOpportunity, GrantRecommendation } from "./grant-types";
export function grantRecommendations(opportunities: GrantOpportunity[], now = new Date()): GrantRecommendation[] { const total=opportunities.reduce((s,o)=>s+o.amount,0); const overdue=opportunities.filter((o)=>new Date(o.deadline)<now); const missing=opportunities.filter((o)=>o.requiredDocuments.some((d)=>d.status==="missing")); const topFunder=Math.max(0,...Object.values(opportunities.reduce<Record<string,number>>((m,o)=>(m[o.funder]=(m[o.funder]??0)+o.amount,m),{}))); return [overdue.length&&{id:"grant-overdue",severity:"critical",title:"Overdue grant deadlines",action:"Chief of Staff should assign recovery or closeout decisions."},missing.length&&{id:"grant-missing-docs",severity:"high",title:"Missing funding documents",action:"Request documents through approved connectors and Knowledge OS metadata."},total<100000&&{id:"grant-low-pipeline",severity:"medium",title:"Low pipeline value",action:"Recommend sourced opportunities for human qualification."},topFunder>total*.6&&{id:"grant-concentration",severity:"medium",title:"Concentration risk",action:"Diversify funder pipeline."}].filter(Boolean) as GrantRecommendation[]; }
