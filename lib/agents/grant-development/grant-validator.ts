import type { GrantOpportunity } from "./grant-types"; import { isRegisteredOpportunityType } from "./grant-registry";
export function validateGrantOpportunity(o: GrantOpportunity){ const errors:string[]=[]; if(!o.organizationId) errors.push("Organization is required."); if(!o.opportunity) errors.push("Opportunity name is required."); if(!isRegisteredOpportunityType(o.type)) errors.push("Opportunity type is not registered."); if(o.amount<0) errors.push("Amount cannot be negative."); if(o.probability<0||o.probability>100) errors.push("Probability must be 0-100."); return errors; }
