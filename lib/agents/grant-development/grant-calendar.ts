import type { GrantDeadline, GrantOpportunity } from "./grant-types";
export function buildGrantDeadlines(opportunities: GrantOpportunity[], now = new Date()): GrantDeadline[] { return opportunities.flatMap((o)=>[{ id:`${o.id}-deadline`, opportunityId:o.id, type: o.stage==="loi"?"loi":o.stage==="reporting"?"reporting":o.stage==="renewal"?"renewal":"application", dueAt:o.deadline, owner:o.owner, status: statusFor(o.deadline, now) as GrantDeadline["status"] }, ...o.reportingRequirements.map((r,i)=>({ id:`${o.id}-report-${i}`, opportunityId:o.id, type:"reporting" as const, dueAt:o.renewalSchedule || o.deadline, owner:o.owner, status: statusFor(o.renewalSchedule || o.deadline, now) as GrantDeadline["status"] }))]); }
function statusFor(date:string, now:Date){ const days=(new Date(date).getTime()-now.getTime())/86400000; return days<0?"overdue":days<=14?"due_soon":"upcoming"; }
