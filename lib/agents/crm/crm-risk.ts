import type { CrmRecommendation, CrmRelationship } from "./crm-types";
export function crmRecommendations(relationships:CrmRelationship[], now=new Date()): CrmRecommendation[] { const stale=relationships.filter((r)=>!["inactive","archived"].includes(r.lifecycleStage) && (now.getTime()-new Date(r.lastActivity).getTime())/86400000>30); const overdue=relationships.filter((r)=>r.followUps.some((f)=>new Date(f.dueAt)<now && f.status!=="complete")); const highValue=relationships.filter((r)=>r.opportunityValue>=50000); const risks=relationships.filter((r)=>r.status==="at_risk"||r.partnershipStatus==="paused"); return [stale.length&&{id:"crm-inactive",severity:"medium",title:"Inactive relationships",action:"Recommend Chief of Staff review and reminder planning."},overdue.length&&{id:"crm-overdue",severity:"high",title:"Overdue follow-ups",action:"Generate reminder-only follow-up report; do not send communications."},highValue.length&&{id:"crm-high-value",severity:"medium",title:"High-value contacts",action:"Prepare deterministic relationship summaries for executive review."},risks.length&&{id:"crm-partnership-risk",severity:"high",title:"Partnership risks",action:"Escalate relationship risk for human decision."}].filter(Boolean) as CrmRecommendation[]; }
