import type { CrmContact, CrmRelationship } from "./crm-types";
export function contactsFromRelationships(relationships: CrmRelationship[]): CrmContact[] { return relationships.map((r)=>({ id:`contact-${r.id}`, relationshipId:r.id, registryType: r.type==="law_firm"?"law_firm":r.type==="media_contact"?"media":r.type==="government_agency"?"government":r.type==="donor"?"donor":r.type==="sponsor"?"sponsor":r.type==="volunteer"?"volunteer":"individual", name:r.organization, role:r.type.replace(/_/g," "), contactInformation:r.contactInformation, tags:r.tags, metadata:{ lifecycleStage:r.lifecycleStage, status:r.status } })); }
