import type { CrmSnapshot } from "./crm-types"; export function crmHealthFromSnapshot(s:CrmSnapshot){ return { status:"healthy", contacts:s.contacts.length, organizations:s.organizations.length, followUps:s.followUps.length, overdueItems:s.overdueFollowUps.length, relationshipHealth:s.dashboard.relationshipHealth, pipeline:s.dashboard.pipeline, telemetry:s.telemetry, message:"CRM Relationship Agent is registered; communications and lifecycle decisions require human approval." }; }
