import { mediaEvents } from "./media-events"; import type { MediaTelemetrySummary } from "./media-types"; export function mediaTelemetrySummary():MediaTelemetrySummary{ return { campaigns:mediaEvents.filter(e=>e.kind==="campaign").length, content:mediaEvents.filter(e=>e.kind==="content").length, approvals:mediaEvents.filter(e=>e.kind==="approval").length, assets:mediaEvents.filter(e=>e.kind==="asset").length, analytics:mediaEvents.filter(e=>e.kind==="analytics").length, risks:mediaEvents.filter(e=>e.kind==="risk").length, recent:mediaEvents.slice(0,10) }; }