import type { MediaSnapshot } from "./media-types"; export function buildMediaRecommendations(s:MediaSnapshot){ return [{id:"media-approval-backlog",severity:"high" as const,title:"Review media approval queue",action:"Route pending content through Approval Engine."}, ...s.content.filter(c=>c.assets.length===0).map(c=>({id:`missing-assets-${c.id}`,severity:"medium" as const,title:`Missing assets for ${c.title}`,action:"Assign asset production before review.",contentId:c.id}))]; }