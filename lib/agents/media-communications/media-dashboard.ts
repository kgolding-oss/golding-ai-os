import type { MediaSnapshot } from "./media-types"; import { contentPipeline } from "./media-content"; import { mediaApprovalQueue } from "./media-approvals"; import { youtubeProduction } from "./media-youtube"; import { podcastProduction } from "./media-podcast"; import { newsletterStatus } from "./media-newsletter"; import { socialMediaQueue } from "./media-social"; import { buildBrandLibrary } from "./media-brand"; import { mediaRisks } from "./media-risk"; export function buildMediaDashboard(s:MediaSnapshot){ return { activeCampaigns:s.campaigns.filter(c=>c.status==="active"), contentPipeline:contentPipeline(s.content), publishingCalendar:s.calendar, youtubeProduction:youtubeProduction(s.content), podcastProduction:podcastProduction(s.content), newsletterStatus:newsletterStatus(s.content), socialMediaQueue:socialMediaQueue(s.content), brandAssets:buildBrandLibrary(s.assets), approvalQueue:mediaApprovalQueue(s.content), productionRisks:mediaRisks(s) }; }