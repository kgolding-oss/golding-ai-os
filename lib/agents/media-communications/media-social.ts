import type { MediaContentItem } from "./media-types"; const social=["instagram","facebook","linkedin","x","tiktok"]; export function socialMediaQueue(content:MediaContentItem[]){ return content.filter(c=>social.includes(c.platform)).map(c=>({ id:c.id,title:c.title,platform:c.platform,hashtags:c.tags.filter(t=>t.startsWith("#")),assets:c.assets,approval:c.approvalStatus,scheduledWindow:c.publishWindow,postingDisabled:true })); }