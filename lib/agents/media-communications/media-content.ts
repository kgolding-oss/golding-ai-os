import type { MediaContentItem, MediaStage } from "./media-types"; import { mediaPipelineStages } from "./media-registry"; export function contentPipeline(content:MediaContentItem[]){ return mediaPipelineStages.map(stage=>({ stage, items:content.filter(i=>i.status===stage) })); } export function requestStageAdvance(item:MediaContentItem,status:MediaStage){ return { ...item, status, updatedAt:new Date().toISOString() }; }