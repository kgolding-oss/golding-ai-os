import type { MediaCampaign, MediaContentItem } from "./media-types"; export function summarizeCampaigns(campaigns:MediaCampaign[], content:MediaContentItem[]){ return campaigns.map(c=>({ ...c, contentCount:content.filter(i=>i.campaignId===c.id||i.campaign===c.name).length, approvalBottlenecks:content.filter(i=>(i.campaignId===c.id||i.campaign===c.name)&&i.approvalStatus==="pending").length })); }