import type { ChiefOfStaffContext, ChiefOfStaffFollowUp } from "./chief-of-staff-types";
export function buildBriefing(ctx: ChiefOfStaffContext) { const now = (ctx.now ?? new Date()).toISOString(); const risks = ctx.executiveIntelligence.risks.filter((r) => r.severity === "critical" || r.severity === "high"); const approvals = ctx.pendingApprovals?.length ?? ctx.dashboard.approvals.filter((a) => a.status === "pending").length; const operatingMode: "normal" | "focused" | "stabilization" | "approval_required" = risks.some((r) => r.severity === "critical") ? "stabilization" : approvals ? "approval_required" : ctx.executiveIntelligence.priorities.length ? "focused" : "normal"; return { id: `cos-brief-${now}`, generatedAt: now, operatingMode, summary: `${ctx.organization?.name ?? "The active organization"} is in ${operatingMode} mode with ${ctx.executiveIntelligence.priorities.length} priorities, ${risks.length} elevated risks, ${approvals} approvals, and ${ctx.executiveIntelligence.opportunities.length} strategic opportunities.`, evidence: [`Executive Intelligence score ${ctx.executiveIntelligence.score.overall}`, `Platform health ${ctx.platformHealth.status}`, `${ctx.workflows.length} workflows`, `${ctx.connectors.length} connectors`, `${ctx.runtimeTools.length} runtime tools`] }; }
export function buildFollowUps(ctx: ChiefOfStaffContext): ChiefOfStaffFollowUp[] { const base = ctx.executiveIntelligence.recommendations.filter((r) => r.status === "open").slice(0, 8); return base.map((r, index) => ({ id: `cos-follow-${r.id}`, title: `Follow up: ${r.title}`, ownerAgent: r.subsystem === "knowledge" ? "knowledge-os" : r.subsystem === "runtime" ? "ai-runtime" : "executive-intelligence", dueAt: new Date((ctx.now ?? new Date()).getTime() + (index + 1) * 86400000).toISOString(), rationale: r.rationale, sourceId: r.id })); }
