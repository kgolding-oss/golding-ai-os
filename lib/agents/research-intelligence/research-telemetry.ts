import type { ResearchTelemetryEvent } from "./research-types"; const events:ResearchTelemetryEvent[]=[]; export const researchTelemetry={record(kind:string,message:string,metadata?:Record<string,unknown>){events.push({id:`research-tel-${events.length+1}`,kind,at:new Date().toISOString(),message,metadata});return events.at(-1)!},summary(){return{events:events.length,recent:events.slice(-10).reverse()}}};
