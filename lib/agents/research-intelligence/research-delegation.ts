import type { ResearchSnapshot } from "./research-types"; export function chiefOfStaffResearchDelegations(s:ResearchSnapshot){return[...s.items.filter(i=>i.status==="queued").map(i=>({type:"research queue review",owner:i.owner,researchItemId:i.id})),...s.citations.filter(c=>c.reviewStatus!=="reviewed").map(c=>({type:"citation verification",owner:"Chief of Staff",researchItemId:c.researchItemId})),...s.monitoring.filter(m=>m.status==="active").map(m=>({type:`${m.category} monitoring`,owner:m.owner,researchItemId:m.id}))];}
