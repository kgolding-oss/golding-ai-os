import type { ResearchItem } from "./research-types"; export function planResearchQueue(items:ResearchItem[]){const rank={critical:0,high:1,medium:2,low:3}; return [...items].sort((a,b)=>rank[a.priority]-rank[b.priority]||String(a.deadline??"").localeCompare(String(b.deadline??""))).map((item,index)=>({step:index+1,researchItemId:item.id,action:`Prepare ${item.topic} for ${item.owner} review`,status:item.status}));}
