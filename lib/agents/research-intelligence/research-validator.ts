import type { ResearchItem, ResearchCitation, ResearchSource } from "./research-types"; import { isResearchCategory } from "./research-registry"; import { validateCitation } from "./research-citations"; export function validateResearchItem(i:ResearchItem){const e:string[]=[]; if(!i.topic)e.push("Research topic is required."); if(!isResearchCategory(i.category))e.push(`Unknown research category ${i.category}.`); if(i.confidence<0||i.confidence>100)e.push("Confidence must be 0-100."); if(i.category==="legal"&&i.reviewStatus==="reviewed")e.push("Legal research still requires human legal review before conclusions."); return e;} export function validateResearchCitations(c:ResearchCitation[],s:ResearchSource[]){return c.flatMap(x=>validateCitation(x,s));}
