import { resolveGoogleWorkspaceAuth } from "./auth";
export class GoogleWorkspaceClient { constructor(readonly service: string, readonly baseUrl: string, readonly scopes: string[]) {} async request<T>(path: string, init: RequestInit = {}): Promise<T> { const auth = resolveGoogleWorkspaceAuth(this.scopes); if (!auth.configured) throw new Error(`Google Workspace auth is not configured for ${this.service}.`); const response = await fetch(`${this.baseUrl}${path}`, { ...init, headers: { Accept: "application/json", ...(init.headers ?? {}) } }); if (!response.ok) throw new Error(`Google Workspace ${this.service} request failed with ${response.status}.`); return response.status === 204 ? ({} as T) : response.json() as Promise<T>; } }
