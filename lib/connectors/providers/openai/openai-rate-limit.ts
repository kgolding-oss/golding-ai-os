const buckets=new Map<string,{count:number;resetAt:number}>(); export function checkOpenAIRateLimit(key:string,limit=60,windowMs=60000){const now=Date.now(); const b=buckets.get(key)??{count:0,resetAt:now+windowMs}; if(now>b.resetAt){b.count=0;b.resetAt=now+windowMs;} b.count++; buckets.set(key,b); return {allowed:b.count<=limit,remaining:Math.max(0,limit-b.count),resetAt:new Date(b.resetAt).toISOString()};}
