import { createConnectorHealth } from "./connector-health";
import type { ConnectorCategory, ConnectorDefinition } from "./connector-types";
const operationIds = ["metadata.read", "health.read", "capabilities.read", "diagnostics.read"];
export function createMockConnector(input: { id: string; name: string; provider: string; category: ConnectorCategory; description: string; resources: string[] }): ConnectorDefinition { const permissions = operationIds.map((id) => ({ id: `${input.id}:${id}`, description: `Allows ${id} for ${input.name}.`, required: true })); return { id: input.id, name: input.name, provider: input.provider, category: input.category, description: input.description, version: "0.1.0", organizationScope: "organization", authentication: { strategies: ["none", "oauth2", "api_key", "bearer_token", "personal_access_token", "jwt", "service_account", "signed_request", "oauth_pkce"], defaultStrategy: "none", requiresUserConsent: false }, permissions, capabilities: [{ id: `${input.id}.mock`, name: `${input.name} metadata`, description: "Deterministic metadata/capability/health surface only.", deterministic: true, resources: input.resources, operations: operationIds }], supportedOperations: operationIds.map((id) => ({ id, name: id, description: `Deterministic ${id} operation.`, permission: `${input.id}:${id}`, deterministic: true, async: false })), supportedResources: input.resources.map((id) => ({ id, name: id, description: `${input.name} ${id} placeholder resource.`, classification: "internal" })), rateLimits: [{ limit: 1000, windowMs: 60000, deterministicBurst: 1000 }], retryPolicy: { maxRetries: 0, backoffMs: 0 }, timeoutMs: 1000, health: createConnectorHealth(), deterministic: true, supportsStreaming: false, supportsAsync: true, supportsWebhooks: false, supportsEvents: true, securityClassification: "internal", execute: (request) => ({ connectorId: input.id, operationId: request.operationId, success: true, output: { connector: input.id, operation: request.operationId, resources: input.resources, deterministic: true, networkCalls: 0 }, durationMs: 0, correlationId: request.context.correlationId }) }; }
