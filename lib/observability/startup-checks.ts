import { hasSupabaseConfig } from "../supabase/config";
import type { DiagnosticFinding } from "./types";
export function runStartupChecks(): DiagnosticFinding[] { const findings: DiagnosticFinding[] = []; if (!process.env.NEXT_PUBLIC_SUPABASE_URL) findings.push({ severity:"warning", subsystem:"environment", id:"NEXT_PUBLIC_SUPABASE_URL", message:"Supabase URL is not configured." }); if (!process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY) findings.push({ severity:"warning", subsystem:"environment", id:"NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY", message:"Supabase publishable key is not configured." }); if (!hasSupabaseConfig()) findings.push({ severity:"warning", subsystem:"supabase", id:"config", message:"Supabase connectivity checks will run in degraded local mode." }); return findings; }
