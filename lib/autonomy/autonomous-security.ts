import type { ExecutionPlan } from "./execution-plan"; export function assertOrganizationIsolation(plan: ExecutionPlan, organizationId?: string | null) { if (plan.organizationId && organizationId && plan.organizationId !== organizationId) throw new Error("Organization isolation violation for autonomous plan."); } export function hasLiveExecutionDisabled() { return true as const; }
