import { autonomousEventBus } from "./autonomous-events"; import { retryEngine } from "./retry-engine"; import { recoveryEngine } from "./recovery-engine"; import { autonomousScheduler } from "./scheduler"; export function getAutonomousTelemetry() { const events = autonomousEventBus.list(); return { events: events.length, recentEvents: events.slice(-10), retryQueue: retryEngine.list().length, recoveryQueue: recoveryEngine.list().length, scheduledPlans: autonomousScheduler.list().length, status: "healthy" as const }; }
