export interface AutonomousCapability { id: string; name: string; permissions: string[]; liveExecutionEnabled: false } export class AutonomousRegistry { private capabilities = new Map<string, AutonomousCapability>(); register(capability: AutonomousCapability) { this.capabilities.set(capability.id, capability); return this; } list() { return Array.from(this.capabilities.values()); } } export const autonomousRegistry = new AutonomousRegistry().register({ id: "autonomy.plan", name: "Create deterministic execution plans", permissions: ["dashboard:read"], liveExecutionEnabled: false }).register({ id: "autonomy.approval", name: "Evaluate approval policies", permissions: ["approvals:read"], liveExecutionEnabled: false });
