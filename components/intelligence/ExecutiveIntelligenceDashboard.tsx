import type { ExecutiveSnapshot } from "../../lib/intelligence";
import { Widget } from "../dashboard/Widget";
export function ExecutiveIntelligenceDashboard({ snapshot }: { snapshot: ExecutiveSnapshot }) {return <section className="grid executiveIntelligence"><Widget eyebrow="Executive Intelligence" title="Single pane of glass"><div className="scoreHero"><strong>{snapshot.score.overall}</strong><span>/100</span><p>{snapshot.score.explanation}</p></div><div className="list">{snapshot.score.categories.map(c=><div className="row" key={c.category}><strong>{c.category}</strong><em>{c.score}</em></div>)}</div></Widget><Widget eyebrow="Top priorities" title="Ranked operating focus"><div className="list">{snapshot.priorities.map(p=><div className="recommendation" key={p.id}><span className={`pill ${p.severity}`}>{p.rank}. {p.severity}</span><h3>{p.title}</h3><p>{p.rationale}</p><strong>{p.recommendedNextStep}</strong></div>)}</div></Widget><Widget eyebrow="Strategic risks" title="Risk register"><div className="list">{snapshot.risks.map(r=><div className="row" key={r.id}><strong>{r.title}</strong><em>{r.severity}</em></div>)}</div></Widget><Widget eyebrow="Opportunities" title="Leverage map"><div className="list">{snapshot.opportunities.map(o=><div className="row" key={o.id}><strong>{o.title}</strong><em>{o.leverage}</em></div>)}</div></Widget><Widget eyebrow="Bottlenecks" title="Constraint map"><div className="list">{snapshot.bottlenecks.length?snapshot.bottlenecks.map(b=><div className="row" key={b.id}><strong>{b.title}</strong><em>{b.blockedArea}</em></div>):<p className="emptyState">No deterministic bottlenecks detected.</p>}</div></Widget><Widget eyebrow="Executive timeline" title="Latest deterministic signals"><p>{snapshot.trendSummary}</p><div className="list">{snapshot.timeline.map(e=><div className="row" key={e.id}><strong>{e.title}</strong><em>{new Date(e.at).toLocaleDateString()}</em></div>)}</div></Widget></section>}
