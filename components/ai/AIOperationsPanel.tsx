export function AIOperationsPanel({score,items}:{score:number;items:Record<string,string|number>}){return <section className="card"><div className="sectionHeader"><div><p className="eyebrow">Executive Dashboard</p><h2>AI Operations</h2></div><span className="statusPill">Score {score}</span></div><div className="grid threeColumn">{Object.entries(items).map(([key,value])=><div key={key}><h3>{key.replace(/([A-Z])/g," $1")}</h3><p>{value}</p></div>)}</div></section>}
